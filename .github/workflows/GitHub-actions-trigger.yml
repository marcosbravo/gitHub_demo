
name: GitHub Actions Trigger
run-name: ${{ github.actor }} is testing out GitHub Trigger 🚀
on: [push]
jobs:
  Trigger-github-actions-demo:
    uses: ./.github/workflows/github-actions-demo.yml
    with:
      variable_random: "X2356!"
